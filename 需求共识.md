# 车载商店 02 · 需求共识

> 这份文档是「我们一起确认下来的需求边界」,作为后续所有实施工作的合同。
> 任何一条要改,我们都先回到这份文档讨论修订,而不是边写边改。
>
> 状态:**已确认**(2026-05-28)
> 实施动作:**待用户下一步指令**

---

## 0. 一句话产品定义

做一个**车载商店 demo**,在车载横屏(1920×720)上让用户浏览商品并完成假支付下单,同时提供一个网页后台让管理员维护商品、分类、推荐位、订单。

**性质**:demo,内部演示给老板/同事看。不是上线产品,不接真实支付,不做物流。

---

## 1. 范围内(必须做)

### 车机端(Android APP)
- [x] 锁定横屏 1920×720,不可旋转,沉浸式(不显示状态栏)
- [x] 首页顶部:**轮播大 Banner**,3–5 张轮播,占屏顶约 1/2 高度
- [x] 首页底部:**分类网格**(磁贴形式)
- [x] 点分类 → 进商品列表页(网格)
- [x] 点商品 → 进商品详情页(左大图,右名称/价格/描述/「下单」按钮)
- [x] 点「下单」→ 弹**二维码**(全屏,带提示「点击屏幕任意位置完成支付」)
- [x] **点屏幕任意位置 = 支付成功** → 显示「支付成功」1.5 秒 → 返回首页
- [x] 商品数据从后端实时拉取,**车机端不存任何数据**

### 后台(网页)
- [x] 单管理员登录:**`admin / Ridemall@Demo2026`**
- [x] 分类管理:增、删、改名、改排序、停用
- [x] 商品管理:增、删、改、上传商品图、选所属分类、设置价格/描述/库存(库存仅展示)
- [x] 推荐位管理:从所有商品里挑 3–5 个放进首页 Banner,可拖拽排序
- [x] 订单查看:倒序列表,看到时间、商品、价格、设备 ID(**手动 F5 刷新**)
- [x] 仪表盘:显示商品数、订单数、营业额、最近 5 笔订单

### 部署
- [x] 部署到家里 Mac mini
- [x] 公网访问 URL:**`https://ridemall.hearagain.space`**
- [x] 复用现有 `panqian-tunnel`,不新建隧道
- [x] 演示完可一键下线(`launchctl unload`)

---

## 2. 明确范围外(不做)

| ❌ 不做 | 原因 |
|---|---|
| 购物车 | 用户明确排除 |
| 真实支付(微信/支付宝/银行卡) | 用户明确排除,demo 性质 |
| 收货地址录入 | 用户明确排除 |
| 物流/配送/订单状态流转 | 用户明确排除 |
| 会员/登录/注册(车机端) | 不需要,单设备匿名 |
| 多管理员/角色权限 | 单管理员够用 |
| 商品搜索框 | demo SKU 几十个,瀑布流够 |
| 复杂分页 | 同上 |
| 多分类标签(一品对多类) | 一个商品只属于一个分类 |
| 多语言 | 仅中文 |
| 离线兜底 | 必须联网使用 |
| 商品评价/评论/收藏 | 范围外 |
| 优惠券/促销/会员价 | 范围外 |
| 多车/多账号同步 | 单设备 demo |

---

## 3. 关键决策(已拍板,不再讨论)

### 产品决策
| 决策点 | 结论 |
|---|---|
| 商品品类 | **车载用品 + 电子配件**(手机支架、充电头、香氛、行车记录仪、USB 线、遮阳挡等) |
| 演示对象 | 老板、同事(内部) |
| 货币显示 | `¥99.00`(中文 ¥ + 两位小数) |
| 轮播节奏 | 4 秒自动切,手动滑后 8 秒静默 |
| Demo 数据 | 系统自动 seed,大致即可,不要求精准 |
| 视觉风格 | **等用户提供设计稿**,实施前先用占位主题跑通流程 |

### 技术决策(我自己定,你不用关心细节,只在出后果时再聊)
| 决策点 | 结论 |
|---|---|
| 后端语言/框架 | Python + Flask + SQLite(跟 panqian_crawler 同构) |
| 车机端语言/框架 | Kotlin + Jetpack Compose |
| 后台 UI | Bootstrap 5 + Jinja2(服务端渲染) |
| 仓库结构 | 单仓 `/server` + `/android` + `/deploy` + `/docs` |
| 图片存储 | 服务器本地文件夹 `server/uploads/`,通过 URL 提供 |
| Android 最低系统版本 | Android 8(minSdk 26) |

### 安全决策(已知风险,用户已确认)
| 决策点 | 结论 | 风险 |
|---|---|---|
| 后台密码 | 固定 `Ridemall@Demo2026` | 公网弱口令风险,**演示完必须下线服务** |
| 防爆破 | 登录失败 5 次/5 分钟/IP 限速兜底 | 仅做基础防护,不上 Cloudflare Access |

---

## 4. 关键业务流程(验收要看的 3 条)

### B1 · 管理员维护商品(后台)
登录 → 创建分类 → 创建商品(传图) → 在「推荐位」页面把商品拖到 Banner → 保存

**验收**:在后台能完成上述动作,刷新页面后数据还在。

### B2 · 用户浏览购买(车机)
打开 APP → 首页轮播 + 分类 → 点分类 → 看商品列表 → 点商品 → 看详情 → 点「下单」→ 弹二维码 → 点屏幕任意处 → 显示「支付成功」→ 回首页

**验收**:整个流程不卡顿、无报错,流畅完成一次完整购买。

### B3 · 订单闭环
车机端完成下单后,管理员在后台「订单」页**手动 F5** 能看到这笔订单,包含时间、商品、价格、设备 ID。

**验收**:车机端下单后,后台刷新页面立刻看到这条新订单。

---

## 5. 验收标准(demo 算「做完」的硬性条件)

1. **公网可访问**:任何能上网的设备打开 `https://ridemall.hearagain.space/admin` 能看到登录页;用 `admin / Ridemall@Demo2026` 能登入。
2. **后台 happy path 通**:管理员能创建分类、创建商品(含上传图)、设置推荐位、看到订单。
3. **车机端 happy path 通**:在 1920×720 Android 模拟器或真车机上,首页 → 分类 → 详情 → 下单 → QR → 点击 → 支付成功 → 回首页,全程无报错。
4. **订单闭环**:车机端下完单,后台订单页 F5 刷新立刻看到新订单。
5. **断网即失败**(预期):车机端没网时打开 APP,显示「网络异常」提示,不崩溃。
6. **下线开关**:能用一条命令(`launchctl unload`)下线整个服务,公网访问立即 404。

---

## 6. 待补充 / 等用户给

以下是**实施开始前还需要等的输入**:

| # | 待给 | 影响什么 | 紧急程度 |
|---|---|---|---|
| 1 | **视觉设计稿**(车机端 + 后台) | Android 的颜色、字体、间距、icon、卡片样式;后台的色系 | 中(无稿可先用占位主题跑通流程) |
| 2 | (可选)商品图片素材 | 如果你不想用 Unsplash 随机图,可以自己给一套 12 张产品图 | 低(可用免费图先顶上) |
| 3 | (可选)品牌名 / Logo | APP 启动图、后台顶部标题 | 低(可先用「Ridemall」占位) |
| 4 | (可选)字体偏好 | 中文字体(默认系统 PingFang / Noto Sans CJK) | 低 |

---

## 7. 不在共识里的灰色地带(以后再说)

下面这些**我们暂时没讨论**,实施过程中如果碰到我会停下来问你,不会自己做主:

- 后台是否需要导出订单到 Excel/CSV?
- 商品详情页是否需要多张图(目前默认单图)?
- 分类是否需要二级子分类?(默认单层)
- 首页除了 Banner + 分类,要不要再加「热销榜」「新品上架」?
- 车机端要不要播放背景音乐 / 音效?
- 后台需要操作日志(谁改了什么时间)?
- 是否需要数据库备份?
- 是否要做 APK 自动更新?

---

## 8. 这份文档的约束力

- 这是**实施时的依据**。任何代码/界面/数据库设计都要先满足这份共识。
- 凡是改动这份共识的,先在文档里改,然后再动手。
- 实施细节(数据表字段、API 路径、文件目录结构、launchd 配置等)在 plan 文件里:
  `~/.claude/plans/demo-app-1-2-3-precious-mitten.md`
  那份文件是给我看的,你不用读。

---

**当前状态**:共识已达成,等你下一步指令(可能是「开工 M1」「先等我发设计稿」「我们再谈某条」「补一个新需求」等)。
